#include <stdio.h>

int main(){
	int num, quadrado = 0;

	printf("Digite um numero: ");
	fflush(stdout);
	scanf("%d", &num);

	quadrado = num * num;

	printf("O quadrado de %d é %d", num, quadrado);

	return 0;
}
