#include <stdio.h>
void soma(int* num, int a, int b, int c){
	*num = a + b + c;
}

int main(){

	int num, a, b,c;

	printf("Digite tres valores inteiros: ");

	fflush(stdout);

	scanf("%d %d %d", &a, &b, &c);

	soma(&num, a, b, c);

	printf("O resultado: %d", num);

	return 0;
}
